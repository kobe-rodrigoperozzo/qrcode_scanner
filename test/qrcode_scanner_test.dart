import 'package:flutter_test/flutter_test.dart';

// ignore: avoid_relative_lib_imports
import '../lib/qrcode_scanner.dart';

void main() {
  test('unit test a', () {
    // expect(class.method(parameter), expected);
  });
}
